# Production target: Postgres.
DATABASE_URL="postgresql://ledger:ledger@localhost:5432/ledger?schema=public"

# Zero-config local fallback: switch the datasource provider in
# prisma/schema.prisma to "sqlite" and point this at a file instead:
# DATABASE_URL="file:./dev.db"
